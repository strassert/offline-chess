# Die Zustandsdatei gehört nicht ins Web – nur die PHP-Dateien lesen sie.
<Files "state.txt">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</Files>
